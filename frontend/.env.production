# Used by `vite build`. Vercel can override with project env; mistaken `*.vercel.app`
# values are ignored in `src/lib/api.ts` so API calls still hit Render.
VITE_API_URL=https://mindbridge-cpva.onrender.com
